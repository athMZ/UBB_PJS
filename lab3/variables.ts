//3. Zdefiniuj po jednym przykładzie zmiennych dostępnych w typescript

let logic: boolean = false; // boolean
let decimal: number = 6; // number
let color: string = "blue"; // string

let list: number[] = [1, 2, 3]; // array of numbers
let x: [string, number]; // tuple
x = ["key", 10];

enum Color { Red, Green, Blue } // enum
let c: Color = Color.Green;

let convert: any = 4; // any
convert = "Konwersja";
convert = false;

let v: void = undefined; // void
let u: undefined = undefined; // undefined
let n: null = null; // null

let obj: object = {}; // object